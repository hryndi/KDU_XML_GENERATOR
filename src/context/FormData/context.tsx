import React from "react";
import type { FormDataContextType } from "../../types/formData";
("../types/formData");

export const FormDataContext = React.createContext<FormDataContextType | undefined>(undefined);
